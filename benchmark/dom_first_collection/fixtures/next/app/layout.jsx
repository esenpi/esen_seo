import '../../shared.css';

export const metadata = { title: 'Collection history benchmark' };

export default function Layout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
